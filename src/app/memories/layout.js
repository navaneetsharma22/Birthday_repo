export const metadata = {
  title: 'Memory Lane | Komal',
  description: 'Some days become photos, some photos become forever.',
};

export default function MemoriesLayout({ children }) {
  return children;
}
