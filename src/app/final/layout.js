export const metadata = {
  title: 'Forever | Komal',
  description: "Even when this little website ends, the love inside it keeps glowing.",
};

export default function FinalLayout({ children }) {
  return children;
}
